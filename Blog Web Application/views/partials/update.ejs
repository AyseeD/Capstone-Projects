<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/myCSSMain.css">
    <title>Blog Site</title>
</head>
<body>
    <%- include("header.ejs") %>

    <% if(locals.Name && locals.Text) {%>
        <form action="/submit" method="post" class="container">
            <input type="hidden" name="postId" value="<%= postId %>">
        
            <div class="mb-3">
                <label for="Name" class="form-label">Name of the Blogger</label>
                <input type="text" class="form-control" name="Name" value="<%= Name %>">
            </div>
            <div class="mb-3">
                <label for="Text" class="form-label">Write Your Blog Here</label>
                <textarea class="form-control" name="Text" rows="3"><%= Text %></textarea>
            </div>
            <button class="btn btn-primary">Submit</button>
        </form>
        
    <% } %>
    

    <%- include("footer.ejs") %>
</body>
</html>